<Window x:Class="MetrikLite.DetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MetrikLite"
        Width="388" SizeToContent="Height" MaxHeight="620"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        Background="Transparent" AllowsTransparency="True"
        Topmost="True" WindowStartupLocation="Manual"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        SnapsToDevicePixels="True" UseLayoutRounding="True"
        Focusable="True"
        Deactivated="OnDeactivated"
        KeyDown="OnKeyDown">
  <Window.Resources>
    <Style x:Key="CircleButton" TargetType="Button">
      <Setter Property="Width" Value="30" />
      <Setter Property="Height" Value="30" />
      <Setter Property="Margin" Value="6,0,0,0" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="FontSize" Value="15" />
      <Setter Property="Foreground" Value="#526174" />
      <Setter Property="Background" Value="#F0F4F9" />
      <Setter Property="BorderBrush" Value="#E2E8F0" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="ButtonBorder" CornerRadius="15"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="ButtonBorder" Property="Background" Value="#E7EEF8" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="ButtonBorder" Property="Background" Value="#DCE7F5" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>
  <Border CornerRadius="22" Background="#F7F9FC" BorderBrush="#DDE5EF" BorderThickness="1"
          Padding="18" Margin="14">
    <Border.Effect>
      <DropShadowEffect BlurRadius="30" ShadowDepth="8" Direction="270"
                        Color="#253750" Opacity="0.22" />
    </Border.Effect>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <Grid Grid.Row="0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Border Width="42" Height="42" CornerRadius="13" Background="#102A5A">
          <Grid>
            <Ellipse Width="31" Height="31" Stroke="#4E8DFF" StrokeThickness="2.2" />
            <TextBlock Text="ML" Foreground="White" FontWeight="SemiBold" FontSize="13"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Grid>
        </Border>
        <StackPanel Grid.Column="1" Margin="11,1,0,0" VerticalAlignment="Center">
          <TextBlock Text="MetrikLite" FontSize="16" FontWeight="SemiBold" Foreground="#172033" />
          <TextBlock x:Name="RefreshedText" FontSize="10.5" Foreground="#7D8A9C" Margin="0,2,0,0" />
        </StackPanel>
        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
          <Button x:Name="RefreshButton" Style="{StaticResource CircleButton}"
                  ToolTip="立即刷新" Click="OnRefreshClick" Content="↻" />
          <Button Style="{StaticResource CircleButton}" ToolTip="关闭"
                  Click="OnCloseClick" Content="×" FontSize="18" />
        </StackPanel>
      </Grid>

      <StackPanel Grid.Row="1" Margin="0,18,0,0">
        <TextBlock Text="CODEX 使用额度" FontSize="10.5" FontWeight="SemiBold"
                   Foreground="#718096" Margin="2,0,0,8" />
        <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="460">
          <StackPanel x:Name="AgentsPanel" />
        </ScrollViewer>
      </StackPanel>

      <Grid Grid.Row="2" Margin="2,4,2,0">
        <TextBlock Text="数据来自本机 Codex · 不上传账户信息"
                   FontSize="10" Foreground="#98A4B4" />
        <Ellipse x:Name="LiveDot" Width="6" Height="6" Fill="#45B97C"
                 HorizontalAlignment="Right" VerticalAlignment="Center" />
      </Grid>
    </Grid>
  </Border>
</Window>
